@model IEnumerable<Publisher>
@{
    ViewData["Title"] = "Index";
}
<div class="row">
    <div class="offset-sm-2 col-6 shadow p-3 mb-5 bg-white rounded">
        <div class="d-flex justify-content-end mb-1">
            <div class="col-11 "><h3>Publisher</h3></div>
            <div class="col-1 justify-content-end">
                <a class="btn btn-primary" asp-controller="Publishers" asp-action="Create"><i class="bi bi-plus-lg"></i></a>
            </div>
        </div>
        <table class="table table-hover table-bordered table-striped">
            <thead class="table-light">
                <tr>
                    <th>@Html.DisplayNameFor(x => x.PublisherName)</th>
                    <th>@Html.DisplayNameFor(x => x.Establish)</th>
                    <th style="width:130px" class="text-center">
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(x => p.PublisherName)</td>
                        <td>@Html.DisplayFor(x => p.Establish)</td>
                        <td class="text-center">
                            <a class="btn btn-success" asp-controller="Publishers" asp-action="Edit" asp-route-id="@p.PublisherId"><i class="bi bi-pencil"></i></a>
                            <a class="btn btn-danger" asp-controller="Publishers" asp-action="Delete" asp-route-id="@p.PublisherId"><i class="bi bi-trash"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section styles {
    <link href="~/libs/icons/font/bootstrap-icons.css" rel="stylesheet" />
}

